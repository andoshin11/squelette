// THIS FILE IS GENERATED BY CODE GENERATOR. DO NOT CHANGE MANUALLY.
/* tslint:disable */
/* eslint-disable */
import { APIRequest, HTTPMethod } from './types'
import * as operations from './gen-ts'

<%_ operations.forEach(operation => { _%>
export class <%= operation.name %> implements APIRequest<operations.<%= operation.name%>Response> {
  response: operations.<%= operation.name%>Response;
  method = HTTPMethod["<%= operation.method %>"];
  path: string;
  params<%= helper.isVoid(operation.queryParameter) && helper.isVoid(operation.requestBody) ? "?" : "" %>: operations.<%= operation.name%>Request

  constructor(args: {
    params<%= helper.isVoid(operation.queryParameter) && helper.isVoid(operation.requestBody) ? "?" : "" %>: operations.<%= operation.name%>Request;
    pathParameter<%= helper.isVoid(operation.pathParameter) ? "?" : "" %>: operations.<%= operation.name%>PathParameter;
  }) {
    const { params, pathParameter } = args
    this.params = params
    this.path = `<%= helper.toTemplateLiteral(operation.path) %>`
  }
}

<%_ }); _%>
